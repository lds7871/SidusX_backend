# GHstart 项目技术概览

**项目名称**: GHstart  
**项目版本**: 1.0-SNAPSHOT  
**包名**: LDS.Person  
**Java版本**: JDK 25  
**最后更新**: 2026年3月20日

---

## 📋 项目概述

GHstart 是一个基于现代化 Spring Boot 框架的中/大型后端项目，采用前后端分离架构。项目使用最新的 Spring Boot 4.0.1 版本，支持 Java 25 语言特性，集成了多种企业级技术方案，包括数据访问、实时通信、API文档、安全管理等功能模块。

---

## 🏗️ 核心技术栈

### 1. **后端框架**
- **Spring Boot**: 4.0.1（最新版本）
  - 父级依赖：`spring-boot-starter-parent`
  - 核心模块集成，包含自动装配和快速启动机制

### 2. **编程语言**
- **Java**: JDK 25
  - Maven 编译版本：25
  - 支持最新的 Java 语言特性和性能优化

### 3. **构建工具**
- **Maven**: Apache Maven 3.x
  - 仓库配置：使用阿里云镜像源（Maven.aliyun.com）
  - 依赖管理：自动版本解析和传递依赖管理

### 4. **IDE 开发辅助**
- **Spring DevTools**: 运行时热重启和热加载
- **Lombok**: 1.18.40
  - 自动生成 getter/setter/toString/equals 等方法
  - 减少样板代码，提高开发效率

---

## 💾 数据管理技术

### 数据库配置
- **数据库类型**: PostgreSQL
- **连接地址**: `jdbc:postgresql://localhost:5432/postgres`
- **JDBC 驱动**: `postgresql:42.7.4`
- **连接管理**: Spring Boot DataSource 自动配置

### 数据访问层框架

#### 1. **MyBatis** (3.0.4)
- **配置类**: `MyBatisConfig.java`
- **Mapper 扫描路径**: `LDS.Person.repository`
- **SQL 映射文件位置**: `classpath:mapper/*.xml`
- **关键配置**:
  ```
  - 下划线转驼峰命名: true
  - 缓存开启: true
  - 懒加载: true
  - 自增主键生成: true
  - 默认执行器类型: REUSE（复用执行器提高性能）
  - SQL 语句超时时间: 30 秒
  - 日志实现: 标准输出 (StdOutImpl)
  ```
- **类型别名包**: `LDS.Person.entity`, `LDS.Person.dto`
- **用途**: 复杂 SQL 查询、动态 SQL、自定义映射

#### 2. **Spring Data JPA**
- **Starter**: `spring-boot-starter-data-jpa`
- **ORM 实现**: Hibernate
- **Dialect**: PostgreSQL
- **DDL 策略**: `validate`（仅验证，不修改结构）
- **SQL 初始化模式**: `never`
- **用途**: 简单 CRUD 操作、关系映射

#### 3. **双 ORM 混合架构**
- **MyBatis**: 处理复杂业务查询、性能优化 SQL
- **JPA/Hibernate**: 处理标准 CRUD、关系维护
- 通过共享 PostgreSQL 数据源和统一的实体定义实现协调

---

## 🌐 Web 框架与 API

### Web 容器
- **Servlet 容器**: Tomcat（Spring Boot 内置）
- **端口**: 8100
- **Context Path**: `/`（根路径，所有请求都直接在根下）

### Web 框架
- **Spring Boot Starter Web**: 完整的 MVC 框架
  - 内置 Dispatcher Servlet
  - 自动配置 RequestMapping 处理
  - 内置 Jackson JSON 序列化

### API 文档与可视化

#### **SpringDoc OpenAPI 2.7.0**（替代 Springfox Swagger）
- 完全兼容 OpenAPI 3.0 规范
- 集成 Swagger UI 可视化界面
- **访问地址**: `http://localhost:8100/swagger-ui.html`
- **API 文档地址**: `http://localhost:8100/v3/api-docs`
- **特点**:
  - 自动扫描 @RestController 和 @RequestMapping
  - 支持 @Operation, @Parameter 注解
  - 自动生成交互式 API 测试界面

### JSON 处理
- **Jackson DataBind**: `com.fasterxml.jackson.core`
  - 自动序列化/反序列化 Java 对象
  - 灵活的 JSON 映射配置

### HTTP 客户端
- **Java 内置 HttpClient**（Java 11+）
  - 工厂类: `HttpClientFactory.java`
  - **模式**: 单例模式，线程安全复用
  - **特点**: 避免重复创建客户端，提高性能
  - **支持特性**: 代理配置、超时管理

- **Spring Boot WebFlux Starter**
  - 异步、非阻塞 HTTP 通信
  - 用于外部 API 调用和异步处理

---

## 🔐 安全机制

### 安全配置类: `SecurityConfig.java`

#### 1. **IP 白名单过滤**
- **启用状态**: `security.ip-whitelist-enabled: true`
- **拦截器**: `IpWhitelistInterceptor.java`
- **白名单列表**:
  ```
  - 127.0.0.1         (本地回环)
  - ::1               (IPv6 本地回环)
  - localhost         (本地主机名)
  - 0:0:0:0:0:0:0:1   (IPv6 完整回环)
  - 115.190.170.56    (服务器公网 IP)
  - 218.89.66.47      (家庭 IP)
  ```
- **作用**: 仅允许指定 IP 地址访问应用
- **环境变量**: 支持环境变量注入配置

#### 2. **Pass Token 绕过机制**
- **启用状态**: `security.pass-token-enabled: true`
- **功能**: 通过有效令牌绕过 IP 白名单限制
- **配置方式**: 环境变量 `${PASS_TOKENS}`（逗号分隔多个令牌）
- **使用场景**: 第三方服务调用、临时访问授权

#### 3. **CORS 跨域配置**
- 配置类: `SecurityConfig` 实现 `WebMvcConfigurer`
- 支持自定义 CORS 映射规则
- 原始框架类 `WebMvcConfig` 已整合

#### 4. **数据验证**
- **Starter**: `spring-boot-starter-validation`
- **实现**: JSR-303/JSR-380 标准
- **用于**: 请求参数验证、业务数据校验

#### 5. **全局异常处理**
- **处理类**: `GlobalExceptionHandler.java`
- **功能**: 统一捕获和处理应用异常
- **返回格式**: 标准化 JSON 错误响应

---

## 📧 外部集成服务

### 邮件发送服务
- **邮箱提供商**: QQ 邮箱 (smtp.qq.com: 587)
- **配置**:
  - SMTP 用户: `1964960588@qq.com`
  - 协议: SMTP with TLS/SSL 加密
  - 超时时间: 5000ms
  - 认证开启: true
  - STARTTLS 必需: true
- **服务类**: `EmailService.java`, `EmailConfig.java`
- **实现**: Spring Boot Mail Starter（未显式列出，由 GlobalYaml 隐含依赖）

---

## 🔌 实时通信

### WebSocket 配置
- **配置类**: `WebSocketConsoleConfig.java`
- **类型**: `@EnableWebSocket` 注解启用
- **功能**:
  - 实时双向通信
  - 控制台输出重定向
  - 密码验证机制
  - 可配置端点路由
- **端点配置**:
  - 默认端点: `/ws/console`
  - 启用开关: `websocket.console.enabled`
  - 密码保护: `websocket.console.password`
- **核心处理器**: `WebSocketConsoleHandler.java`
- **用途**: 实时日志、控制台交互、系统监控

---

## 📊 日志管理

### 日志框架体系
- **Starter**: `spring-boot-starter-logging`
- **选择**: SLF4J + Logback（Spring Boot 默认集成）
- **配置文件**: `logback-spring.xml`
- **日志输出位置**: 控制台 + 文件

### 日志级别配置
```
root: INFO                                    # 全局日志级别
LDS.Person: DEBUG                             # 项目代码调试日志
org.springframework.web: WARN                 # Spring Web 框架日志
org.springframework.web.servlet.*: WARN       # DispatcherServlet 日志
org.apache.coyote*: WARN                      # Tomcat HTTP 处理日志
org.apache.tomcat: WARN                       # Tomcat 容器日志
org.hibernate.SQL: DEBUG                      # Hibernate SQL 日志
org.mybatis: DEBUG                            # MyBatis 执行日志
```

### 专业日志过滤
- **ApiLogFilter.java**: 截断 API 请求/响应日志
- 记录所有 HTTP 请求的详细信息
- 用于审计、监控、故障排查

### 输出格式
- 时间戳、日志级别、线程名、类名、日志消息
- 彩色控制台输出支持

---

## 🏢 应用目录结构

```
src/main/java/LDS/Person/
├── GHstartApplication.java              # 应用启动类
├── config/                               # 配置模块
│   ├── ApiLogFilter.java                 # API 日志拦截过滤
│   ├── BypassIpWhitelist.java           # IP 白名单绕过处理
│   ├── ConfigManager.java                # 配置管理器
│   ├── EmailConfig.java                  # 邮件配置
│   ├── GlobalExceptionHandler.java      # 全局异常处理
│   ├── HttpClientFactory.java            # HTTP 客户端工厂
│   ├── IpWhitelistInterceptor.java      # IP 白名单拦截器
│   ├── MyBatisConfig.java                # MyBatis ORM 配置
│   ├── SecurityConfig.java               # 安全配置（含 CORS）
│   ├── SecurityFilter.java               # 安全过滤器
│   ├── SwaggerConfig.java                # API 文档配置
│   ├── WebSocketConsoleConfig.java       # WebSocket 配置
│   └── WebSocketConsoleHandler.java      # WebSocket 处理器
├── controller/                           # REST API 控制层（MVC）
│   ├── AnnouncementController.java       # 公告管理 API
│   ├── ArticleController.java            # 文章管理 API
│   ├── DeepSeekController.java           # DeepSeek AI 调用 API
│   ├── MsShipController.java             # 航天/舰船数据 API
│   ├── NasaDailyImageController.java     # NASA 每日图片 API
│   ├── RecentLaunchController.java       # 最近发射任务 API
│   ├── ServerInfoController.java         # 服务器信息 API
│   ├── UserController.java               # 用户管理 API
│   ├── WikiController.java               # Wiki 百科 API
│   ├── WikiCommentController.java        # Wiki 评论 API
│   ├── WikiNewController.java            # Wiki 新建 API
│   └── WikiReviewController.java         # Wiki 审核 API
├── dto/                                  # 数据传输对象
│   ├── request/                          # 请求 DTO
│   └── response/                         # 响应 DTO
├── entity/                               # ORM 实体类
│   ├── Announcement.java                 # 公告实体
│   ├── ApiLog.java                       # API 日志实体
│   ├── Article.java                      # 文章实体
│   ├── FalconStats.java                  # Falcon 统计实体
│   ├── MsShip.java                       # 航天舰船实体
│   ├── NasaDailyImage.java               # NASA 图片实体
│   ├── RecentLaunch.java                 # 发射任务实体
│   ├── User.java                         # 用户实体
│   ├── Wiki.java                         # Wiki 主条目实体
│   ├── WikiComment.java                  # Wiki 评论实体
│   ├── WikiHistory.java                  # Wiki 历史版本
│   ├── WikiNew.java                      # Wiki 新建草稿
│   └── WikiReview.java                   # Wiki 审核记录
├── repository/                           # 数据访问层
│   └── *Mapper.java                      # MyBatis Mapper 接口
├── service/                              # 业务逻辑层
│   ├── *Service.java                     # 服务接口
│   ├── impl/                             # 服务实现类
│   └── VerifyCodeStore.java              # 验证码存储
├── sys/                                  # 系统模块（待补充）
├── tasks/                                # 任务调度模块
│   └── 定时任务、后台任务处理
└── util/                                 # 工具类库
```

---

## 📝 业务功能模块

### 1. **用户管理模块**
- 用户认证、授权、个人资料管理
- 课程：`UserService.java`, `UserController.java`

### 2. **文章/内容管理**
- 文章发布、编辑、删除
- 类：`ArticleService.java`, `ArticleController.java`

### 3. **Wiki 知识库**
- 分布式百科全书功能
- 支持评论、审核、版本历史
- 核心类：
  - `WikiService.java` - Wiki 主条目
  - `WikiCommentService.java` - 评论管理
  - `WikiNewService.java` - 新建草稿
  - `WikiReviewService.java` - 审核流程

### 4. **公告系统**
- 发布重要通知
- 类：`AnnouncementService.java`, `AnnouncementController.java`

### 5. **外部数据集成**
- **NASA Daily Image** - 每日天文图片API集成
  - 类：`NasaDailyImageService.java`
- **航空航天数据** - SpaceX、Blue Origin 任务数据
  - 类：`RecentLaunchService.java`, `FalconStatsService.java`
- **航海数据** - MsShip 舰船信息
  - 类：`MsShipService.java`

### 6. **AI 集成**
- DeepSeek AI 调用接口
- 类：`DeepSeekController.java`

### 7. **系统监控**
- 服务器信息、性能监控
- 类：`ServerInfoController.java`

### 8. **邮件服务**
- SMTP 邮件发送
- 类：`EmailService.java`

### 9. **验证码服务**
- 一次性验证码生成、验证
- 类：`VerifyCodeStore.java`

---

## 💻 会话管理

### Session 配置
```yaml
spring.session.store-type: memory      # 内存存储（开发环境）
spring.session.timeout: 1800           # 30 分钟自动过期
spring.session.cookie.name: JSESSIONID # 标准 Servlet Cookie 名
spring.session.cookie.http-only: true  # 防止 JavaScript 访问
spring.session.cookie.secure: false    # 本地开发禁用 HTTPS 强制
spring.session.cookie.max-age: 1800    # Cookie 最大存活时间
spring.session.cookie.path: /          # Cookie 路径
```

**生产环境建议**:
- 使用 Redis 替代内存存储（`spring.session.store-type: redis`）
- 启用 `secure: true` 和 `same-site: strict`
- 使用 HTTPS 传输

---

## 📚 依赖管理

### Maven 仓库配置
- **阿里云镜像源**: 加速国内依赖下载
  - 公共仓库: `https://maven.aliyun.com/repository/public`
  - Spring 仓库: `https://maven.aliyun.com/repository/spring`
  - 插件仓库: `https://maven.aliyun.com/repository/public`

### 主要依赖版本清单
| 组件 | 版本 | 说明 |
|------|------|------|
| Spring Boot | 4.0.1 | 最新主流版本 |
| Java | 25 | 最新 LTS 以上版本 |
| MyBatis | 3.0.4 | Spring Boot 4 适配版 |
| PostgreSQL | 42.7.4 | 最新 JDBC 驱动 |
| SpringDoc OpenAPI | 2.7.0 | Swagger 替代方案 |
| Lombok | 1.18.40 | 最新稳定版 |
| Jackson | (继承自 parent) | JSON 序列化 |

---

## 🚀 启动与运行

### 启动入口
```java
@SpringBootApplication
@EnableScheduling             // 启用定时任务
public class GHstartApplication {
    public static void main(String[] args) {
        SpringApplication.run(GHstartApplication.class, args);
        System.out.println("GHstart 应用启动成功！");
        System.out.println("访问地址: http://localhost:8100");
    }
}
```

### 启动步骤
1. **编译**: `mvn clean compile`
2. **打包**: `mvn clean package`
3. **运行**: `java -jar target/GHstart-1.0-SNAPSHOT.jar`
4. **访问**:
   - 主应用: `http://localhost:8100`
   - API 文档: `http://localhost:8100/swagger-ui.html`
   - WebSocket: `ws://localhost:8100/ws/console`

### 快速启动脚本
- `A编译.cmd` - Maven 编译
- `A启动.cmd` - 启动应用
- `A新版终端启动.cmd` - 新终端中启动
- `B更新依赖.cmd` - 更新 Maven 依赖
- `B快速配置.cmd` - 快速配置脚本
- `B同步项目.cmd` - 项目同步
- `C部署后安全测试.cmd` - 部署后安全验证

---

## 🔍 技术特点总结

### ✅ 现代化技术栈
- 使用 Spring Boot 4.0.1（2024-2025 最新版本）
- Java 25 语言特性和性能优化
- 双 ORM 框架混合（MyBatis + JPA），各取所长

### ✅ 企业级安全机制
- IP 白名单 + Pass Token 两层访问控制
- 全局异常处理和统一错误响应
- CORS 跨域配置
- 数据验证框架集成

### ✅ 完整的系统集成
- REST API + WebSocket 双通道通信
- 第三方 API 集成（NASA、SpaceX、航海数据）
- AI 服务集成（DeepSeek）
- 邮件服务集成

### ✅ 开发者友好
- Swagger/OpenAPI 自动 API 文档
- 实时日志记录和分析
- Spring DevTools 热重载
- Lombok 减少样板代码

### ✅ 生产就绪
- MyBatis 缓存和懒加载
- HTTP 客户端单例复用
- 定时任务调度
- PostgreSQL 企业数据库

---

## 📖 相关配置文件位置

| 配置文件 | 路径 | 说明 |
|---------|------|------|
| 应用配置 | `src/main/resources/application.yml` | Spring Boot 主配置 |
| 本地配置 | `src/main/resources/config.properties` | 本地自定义配置 |
| 日志配置 | `src/main/resources/logback-spring.xml` | Logback 日志配置 |
| 数据库脚本 | `src/main/resources/database.sql` | 初始化 SQL 脚本 |
| MyBatis Mapper | `src/main/resources/mapper/*.xml` | SQL 映射文件 |
| Nginx 配置 | `Nginx配置.txt` | 反向代理配置 |

---

## 📌 后续优化建议

1. **安全加强**：
   - 集成 Spring Security 框架
   - 实现 JWT 或 OAuth2 认证
   - 敏感数据加密存储

2. **性能优化**：
   - Redis 缓存集成
   - 数据库查询优化和索引
   - 异步消息队列（RabbitMQ/Kafka）

3. **监控与可观测性**：
   - 集成 Micrometer + Prometheus 监控
   - ELK 日志聚合分析
   - 分布式链路追踪（Jaeger/Sleuth）

4. **高可用部署**：
   - Docker 容器化
   - Kubernetes 编排
   - 负载均衡和多实例部署

5. **代码质量**：
   - 单元测试和集成测试
   - SonarQube 代码质量检查
   - CI/CD 自动化流程

---

**文档生成时间**: 2026年3月20日  
**项目状态**: 开发/维护中  
**维护者**: GHstart Team  
**许可证**: MIT（推荐）
